pipeline{
    agent any
    stages{
        stage('first'){
            steps{
                sh 'printenv'
                echo "Running ${env.BUILD_NUMBER} on ${env.xinjiahui}"
            }
        }
    }
}